version: "3"

services:
  foo:
    labels:
      kompose.service.type: headless
    deploy:
      mode: global
      replicas: 6
      labels:
        com.example.description: "This label will appear on the web service"
    image: redis
