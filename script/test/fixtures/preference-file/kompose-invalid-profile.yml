profiles:
  default:
    provider: kubernetes
    objects:
      - deployment
      - replicationcontroller

  test:
    provider: openshift
    objects:
      - deploymentconfig

current-profile: unknownprofile
